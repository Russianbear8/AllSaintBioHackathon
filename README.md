# AllSaintBioHackathon

- Run `docker compose up --build`
- Then in browser open `http://localhost:4173/`
- Click `Test` button
- Click `Submit` button


#### Note
- Docker compose will take a couple of minutes
- After submitting the request, the request will run for around 1 minute